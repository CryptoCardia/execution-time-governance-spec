# Test vectors

Each vector includes:
- ector-XX-*.json the inputs
- ector-XX-expected.json expected verification result

These vectors validate:
- minimal happy path
- replay semantics (logical)
- parameter mutation detection (hash mismatch)
- expiry enforcement

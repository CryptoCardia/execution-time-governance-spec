# ETG v1 Reference Verifier (TypeScript)

This is a minimal reference verifier for ETG v1.

It demonstrates:
- canonicalization
- envelope hashing
- AO binding checks
- time window enforcement
- deterministic error codes

It is not production-ready. It does not implement rail integrations or policy engines.

## Usage
1. Install deps
2. Run verify on a test vector

